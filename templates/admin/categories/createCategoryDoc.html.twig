{% extends 'base.html.twig' %}

{% block title %}Admin - Incyte{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('admin') }}
{% endblock %}
{% block body %}

    {% include '/admin/_navbar.html.twig' %}
    <style>label.required{display: contents}</style>
    <div class="container">
        <h3 class="pb-4"><a class="pr-3" href="{{ path('admin_categorie') }}" >&#8249;</a>Catégorie de documents</h3>
        <div class="row bg-white p-3 shadow-lg rounded">
            <div class="col-12">
                {% for message in app.flashes('sucess') %}
                    <div class="alert alert-success">
                        {{ message }}
                    </div>
                {% endfor %}
                {{ form(form) }}
            </div>
        </div>
    </div>

{% endblock %}

