<form method="post" action="{{ path('admin_congres_delete', {'id': congre.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce congres?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ congre.id) }}">
    <a class="dropdown-item" ><button class="btn btn-delete p-0">Supprimer</button></a>
</form>
