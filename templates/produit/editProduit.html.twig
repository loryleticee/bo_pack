{% extends 'base.html.twig' %}

{% block title %}Admin - Incyte{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('admin') }}
{% endblock %}
{% block body %}

    <style>label.required{display: contents}</style>
    <td><a href="{{ path( 'admin_produit' ) }}"><button class="btn btn-primary">Retour à la liste d'utilisateur</button></a></td>
    <div class="container">
        <div class="row bg-white p-3 shadow-lg">
            <div class="col-12">
                {% for message in app.flashes('sucess') %}
                    <div class="alert alert-success">
                        {{ message }}
                    </div>
                {% endfor %}

                {{ form_start(form) }}
                <div class="my-custom-class-for-errors">
                    {{ form_errors(form) }}
                </div>

                <div class="row">
                    <div class="col">
                        {{ form_row(form.name) }}
                        {{ form_row(form.brand) }}
                    </div>
                    <div class="col">
                        {{ form_row(form.model) }}
                        {{ form_row(form.status) }}
                        {{ form_row(form.type) }}
                    </div>
                </div>
                {{ form_row(form.place) }}

                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}

